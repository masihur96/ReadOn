import 'package:flutter/material.dart';

class HomePageEbook extends StatelessWidget {
  const HomePageEbook({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
